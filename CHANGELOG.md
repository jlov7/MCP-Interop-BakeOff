# Changelog

## Unreleased

- Rebuilt the project as a `src/` Python package with CLI, local API, MCP server, and web app.
- Added deterministic simulator runs, hermetic artifact roots, run manifests, and redaction.
- Added Streamable HTTP and stdio JSON-RPC protocol tests.
- Added local web console with matrix, trace, comparison, artifact, docs, and live run surfaces.
- Added CI, security documentation, contribution guide, issue templates, Docker, and devcontainer.
- Added official MCP conformance smoke, vendor SDK readiness checks, side-by-side artifact diffs,
  optional local API bearer auth, and a golden release demo generator.
